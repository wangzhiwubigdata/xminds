1.5W训练数据，5000预测数据
预处理：统计训练集预测集样本缺失数目，删掉缺失值大于194的样本，添加样本特征缺失数目特征，添加样本缺失数目所在阶梯特征
特征工程：对特征排序，离散化，归一化，取log，缺失值中位数填充，多项式组合
特征选择：xgboost训练模型对特征排序，取topk，计算特征的相似度，去除冗余特征
模型：RF,SVM,XGBOOST 进行bagging融合，加权融合

5W无标签数据
先做样本缩减，不是聚类，邻域粗糙集缩减，在邻域范围类缩减
10个样本一组，随机打标，添加到1500训练集，13500测试集做测试，保留提高最多的一组标签
所有样本均按照此方式打标，保留topk的标签组
将这些打标的数据随机添加到前面的数据中，按照前面的方式训练预测
